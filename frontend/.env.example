# Backend API base URL consumed by the React app.
# Loaded automatically per environment:
#   npm start      -> .env.development
#   npm run build  -> .env.production
REACT_APP_API_URL=http://localhost:8000
